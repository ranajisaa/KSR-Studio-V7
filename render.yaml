services:
  - type: web
    name: ksr-studio-v8
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: VEO_MODEL
        value: veo-3.1-generate-preview
